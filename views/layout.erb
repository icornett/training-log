<!doctype html>
<html lang="en-US">
  <head>
    <title>Training Log</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Teko:wght@500;600&family=Work+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/training_log_app.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  </head>

  <body>
    <header>
      <div class="topnav">
      <% if session[:username] %>
        <form action="/delete_account" method="post">
          <button class="button" type="submit">
            <i class="fa fa-trash-o fa-2x" aria-hidden="true"></i>
          </button>
        </form>
        <form action="/logout" method="post">
          <button class="button" type="submit">
            <i class="fa fa-sign-out fa-2x" aria-hidden="true"></i>
          </button>
        </form>
        <form action="/training_log/1/workouts" method="get">
          <button class="button" type="submit">
            <i class="fa fa-home fa-2x" aria-hidden="true"></i>
          </button>
        </form>
      <% end %>
      </div>
      <h1>Training Log</h1>
    </header>

    <% if session[:message] %>
      <div id="flash">
        <p><%= session.delete(:message) %></p>
      </div>
    <% end %>

    <main>
      <%== yield %>
    </main>
  </body>
</html>
