---
# Brakeman SAST configuration
# https://brakemanscanner.org/docs/configuration/

app_path: "."

# Exit with non-zero status if any warnings are found
exit_on_warn: true

# Suppress informational output when run in CI
quiet: false

# Output formats produced by CI (--format github --format json)
# Local dev: brakeman reads this file; pass --format github for annotations.
